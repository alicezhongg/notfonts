import React from "react";

const FontBox = (props) => {
  return (
    <div className="border bg-[#000000] text-white text-8xl">
      <h1>{props.name}</h1>
    </div>
  );
};

export default FontBox;
